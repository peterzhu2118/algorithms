package ca.peterzhu.algorithms;

/**
 * Miracle Sort. That is all.
 *
 * Time Complexity - Best Case: O(?) -> Even if all elements are sorted, we still wait for a miracle.
 * Time Complexity - Worst Case: O(?) -> Wtf
 * Time Complexity - Average: O(?) - > I actually have no idea
 *
 * @author Andrew Peng
 */
public class MiracleSort {
	
	/**
	 * Sorts the array using miracle sort.
	 * 
	 * @param arr the array to be sorted
	 */
	public static void sort(int[] arr) {

        boolean sorted = false;

		while (!sorted) {
            // Wait for miracle to happen
		}

        // Once miracle happens, the array is sorted

	}
	
}